// Plantilla: copia este archivo como "credentials.h" y pon tus datos reales.
// credentials.h está en .gitignore, así que tu contraseña nunca se sube a git.

const char* WIFI_SSID = "TU_RED_WIFI";
const char* WIFI_PASSWORD = "TU_CONTRASEÑA";
